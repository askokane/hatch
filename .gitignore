/node_modules
/.next/
/out/
next-env.d.ts
/build
.env
.env*.local
/prisma/dev.db
/prisma/dev.db-journal
/e2e-test.db
*.db
*.db-journal
/test-results/
/playwright-report/
/playwright/.cache/
# Runtime handle for the e2e server, written by e2e/server-control.ts. Transient
# by nature, and a committed one would name a PID from someone else's machine —
# which the harness now reads as "an orphan of mine is holding the port".
/e2e/.server.pid
.DS_Store
*.pem
npm-debug.log*
dev-server.log
*.tsbuildinfo
.vercel

# Pre-migration handle snapshots (contain user data)
handle-backup-*.json
snapshot-handles.mjs
