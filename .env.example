# --- HATCH environment ---

# SQLite for local dev. To switch to Postgres later: change the datasource
# provider in prisma/schema.prisma to "postgresql" and set this to a Postgres URL.
DATABASE_URL="file:./dev.db"

# Registration requires a .edu email. For local dev / seed / e2e accounts you can
# bypass that with an allowlist: comma-separated full emails and/or "@domain" entries.
# This bypass is only honored when NODE_ENV !== "production".
DEV_EMAIL_ALLOWLIST="@stateu.edu,@hatchdemo.edu"

# Base URL used when printing dev verification / password-reset links to the console.
APP_URL="http://localhost:3000"
